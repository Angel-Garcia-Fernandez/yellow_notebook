es:
  activerecord:

    attributes:
      teacher:
        default_attendance_management: attendance
        default_collection_management: collection

    errors:
      models:
        account_detail:
          attributes:
            default_account:
              one_default: must be one and only one account by default
        time_week_cycle:
          attributes:
            activity_class_ends_at:
              ends_before_start: can't end before start
            period_ended_at:
              ended_before_start: can't end before start
        teacher_activity:
          attributes:
            teacher_in_charge:
              one_default: must be one and only one teacher in charge