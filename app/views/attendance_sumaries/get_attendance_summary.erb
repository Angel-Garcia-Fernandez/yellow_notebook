<%= form_for(@attendance_sumary) do |f| %>
    <% if @attendance_sumary.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@attendance_sumary.errors.count, "error") %> prohibited this attendance_sumary from being saved:</h2>

          <ul>
            <% @attendance_sumary.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
