<%= form_for @xls_file, html: { class: 'form-horizontal' } do |f| %>
    <div class="form-group">
      <%= f.label :file, class: 'control-label col-md-2' %>
      <div class="col-md-10">
        <%= f.file_field :file, class: 'text_field form-control' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.submit nil, class: 'btn btn-primary' %>
      <%= link_to t('.cancel', default: t("helpers.links.cancel")),
                  xls_files_path, class: 'btn btn-default' %>
    </div>
<% end %>
