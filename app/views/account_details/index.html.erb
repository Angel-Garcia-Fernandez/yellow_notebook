<%- model_class = AccountDetail -%>
<div class="page-header">
  <h1><%=t '.title', default: model_class.model_name.human( count:2 ).titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id).capitalize %></th>
      <th><%= model_class.human_attribute_name(:iban).capitalize %></th>
      <th><%= model_class.human_attribute_name(:default_account).capitalize %></th>
      <th><%= model_class.human_attribute_name(:guardian_id).capitalize %></th>
      <th><%= model_class.human_attribute_name(:student_id).capitalize %></th>
      <th><%= model_class.human_attribute_name(:created_at).capitalize %></th>
      <th><%=t '.actions', default: t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @account_details.each do |account_detail| %>
      <tr>
        <td><%= account_detail.id %></td>
          <td><%= account_detail.iban %></td>
          <td><%= account_detail.default_account %></td>
          <td><%= account_detail.guardian_id %></td>
          <td><%= account_detail.student_id %></td>
        <td><%=l account_detail.created_at, format: :long %></td>
        <td>
          <%= link_to account_detail_path(account_detail), class: 'btn btn-xs', title: "#{ t('.show', default: t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
          <%- end -%>
          <%= link_to edit_account_detail_path(account_detail), class: 'btn btn-xs', title: "#{ t('.edit', default: t('helpers.links.edit')) }" do %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to account_detail_path(account_detail), method: :delete, data: { confirm: t('.confirm', default: t("helpers.links.confirm", default: 'Are you sure?')) }, class: 'btn btn-xs', title: "#{ t('.destroy', default: t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', default: t("helpers.links.new")),
            new_account_detail_path,
            class: 'btn btn-primary' %>
